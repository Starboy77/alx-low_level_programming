#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int num = 0;
while (num < 10)
{
if (num < 9)
{
putchar((num) + '0');
putchar(',');
putchar(' ');
num++;
}
else
{
putchar((num) + '0');
n++;
}

}
return (0);
}
