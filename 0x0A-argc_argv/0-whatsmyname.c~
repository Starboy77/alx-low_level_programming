#include "main.h"
#include <stdio.h>

/**
 * main - main function
 * @argc: counts argument
 * @argv: list of argument
 * Return: 0
 */

int main(int argc, char **argv)
{
	__attribute__((unused)) argc;
	printf("%s\n", argv[0]);
	return (0);
}
